// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update this to your real domain once you connect one in Cloudflare.
  // It is used to build correct absolute URLs (e.g. in the RSS feed/sitemap).
  site: 'https://raul-website.pages.dev',
});
